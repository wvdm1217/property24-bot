{{- if .Values.payload.useConfigMap }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Values.payload.configMapName }}
  labels:
    {{- include "property24-bot.labels" . | nindent 4 }}
data:
  payload.json: |
{{- if .Values.payload.jsonContent }}
{{ .Values.payload.jsonContent | nindent 4 }}
{{- end }}
{{- end }}